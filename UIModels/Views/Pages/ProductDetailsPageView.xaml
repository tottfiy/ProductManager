<UserControl x:Class="UIModels.Views.Pages.ProductDetailsPageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Loaded="OnLoaded"
             DataContextChanged="OnDataContextChanged">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0"
                    Orientation="Horizontal"
                    Margin="0,0,0,16">
            <Button Content="← Назад"
                    Padding="12,6"
                    Margin="0,0,8,0"
                    Command="{Binding BackCommand}" />
            <Button Content="Редагувати"
                    Padding="12,6"
                    Margin="0,0,8,0"
                    Command="{Binding EditCommand}" />
            <Button Content="Скасувати"
                    Padding="12,6"
                    Margin="0,0,8,0"
                    Command="{Binding CancelEditCommand}" />
            <Button Content="Зберегти"
                    Padding="12,6"
                    Margin="0,0,8,0"
                    Command="{Binding SaveCommand}" />
            <Button Content="Оновити"
                    Padding="12,6"
                    Margin="0,0,8,0"
                    Command="{Binding RefreshCommand}" />
            <Button Content="Видалити товар"
                    Padding="12,6"
                    Command="{Binding DeleteProductCommand}" />
        </StackPanel>

        <Border Grid.Row="1"
                Padding="16"
                Background="#FFF8F8F8"
                CornerRadius="8"
                BorderBrush="#FFE1E1E1"
                BorderThickness="1">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="150" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Режим" FontWeight="SemiBold" Margin="0,0,12,10" />
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Title}" FontSize="20" FontWeight="Bold" Margin="0,0,0,10" />

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="ID" FontWeight="SemiBold" Margin="0,0,12,10" />
                    <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding DisplayId}" Margin="0,0,0,10" />

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="Склад" FontWeight="SemiBold" Margin="0,0,12,10" />
                    <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding WarehouseName}" Margin="0,0,0,10" />

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="Назва" FontWeight="SemiBold" Margin="0,0,12,10" />
                    <TextBox Grid.Row="3" Grid.Column="1" Height="34" Padding="10,6" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding IsReadOnlyMode}" Margin="0,0,0,10" />

                    <TextBlock Grid.Row="4" Grid.Column="0" Text="Категорія" FontWeight="SemiBold" Margin="0,0,12,10" />
                    <ComboBox Grid.Row="4" Grid.Column="1" Height="34" ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" IsEnabled="{Binding IsEditMode}" Margin="0,0,0,10" />

                    <TextBlock Grid.Row="5" Grid.Column="0" Text="Кількість" FontWeight="SemiBold" Margin="0,0,12,10" />
                    <TextBox Grid.Row="5" Grid.Column="1" Height="34" Padding="10,6" Text="{Binding QuantityText, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding IsReadOnlyMode}" Margin="0,0,0,10" />

                    <TextBlock Grid.Row="6" Grid.Column="0" Text="Ціна" FontWeight="SemiBold" Margin="0,0,12,10" />
                    <TextBox Grid.Row="6" Grid.Column="1" Height="34" Padding="10,6" Text="{Binding PriceText, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="{Binding IsReadOnlyMode}" Margin="0,0,0,10" />

                    <TextBlock Grid.Row="7" Grid.Column="0" Text="Загальна вартість" FontWeight="SemiBold" Margin="0,0,12,10" />
                    <TextBlock Grid.Row="7" Grid.Column="1" Text="{Binding TotalValuePreview, StringFormat={}{0:C2}}" Margin="0,0,0,10" />

                    <TextBlock Grid.Row="8" Grid.Column="0" Text="Опис" FontWeight="SemiBold" Margin="0,0,12,10" />
                    <TextBox Grid.Row="8"
                             Grid.Column="1"
                             Height="110"
                             Padding="10,8"
                             AcceptsReturn="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}"
                             IsReadOnly="{Binding IsReadOnlyMode}" />
                </Grid>
            </ScrollViewer>
        </Border>

        <Border Grid.RowSpan="2"
                Background="#66FFFFFF"
                Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Width="260">
                <ProgressBar Height="16"
                             IsIndeterminate="True" />
                <TextBlock Text="{Binding BusyMessage}"
                           Margin="0,12,0,0"
                           HorizontalAlignment="Center"
                           FontWeight="SemiBold" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
